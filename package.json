{
  "name": "beegloo-guide",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "dev": "vite",
    "creative": "node --env-file-if-exists=.env tools/creative/generate.mjs",
    "prepare:assets": "node scripts/prepare-public-assets.mjs",
    "predev": "npm run prepare:assets",
    "build": "npm run prepare:assets && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.1.1",
    "sharp": "^0.35.4",
    "vite": "^8.2.2"
  }
}
